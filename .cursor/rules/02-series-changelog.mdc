---
description: 系列文章更新必须记入 SERIES_CHANGELOG
globs: "**/0[1-8]-卷*/**/*.{md,MD}"
alwaysApply: false
---

# 系列更新记录（强制）

凡对卷内系列正文做 **新增 / 重写 / 结构性大改 / 规范升级（v5→v6）/ 删除或合并**，同一回合内必须追加：

`00-Meta/harness/SERIES_CHANGELOG.md`

## 条目模板（追加到文件顶部「最新」区下方）

```markdown
### YYYY-MM-DD · <系列短名> · <动作>
- **路径**：`卷/章/文件.md`
- **动作**：新增 | 重写 | 大改 | 规范升级 | 删除/合并
- **摘要**：一句话 why
- **规范**：v6 §0 自检 □ / verify 脚本 □
- **关联**：长任务 ID `LT-xxx` / 债务 ID `TD-xxx` / 缺口关闭 □
```

## 动作判定

| 改动 | 要记账？ |
|:---|:---:|
| 新写一节/一篇 | 是 |
| 整篇按 v6 重写或升 AOSP 17 | 是 |
| 改导航 index/README 结构 | 是（注明「结构」） |
| 错别字、坏链、标点 | 否（可写周汇总） |
| 仅改 `site/` 或 `docs/` 生成物 | 否 |

## 同步

- 关闭某章「待撰写」→ 更新 `00-Meta/缺口一览.md` 与章 `index.md` 状态。
- 偿还质量债务 → 在 `TECH_DEBT.md` 将该条标 `done`。
