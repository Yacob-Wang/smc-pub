---
description: 书章节续写约定——默认体例、样板路径、禁止系列长文体串台
globs: "**/0[1-6]-卷*/**/*.md"
alwaysApply: false
---

# 书章节续写（卷内 `N.M-*.md`）

打开本仓库卷内正文时默认适用。完整硬约束见 `01-writing-standards.mdc`。  
**卷/章定位边界见 `05-chapter-positioning.mdc`（always）——主题不符本章则不要写在这里。**

## 续写前必读

1. 目标章 `index.md` **章定位**（主题必须落入核心子节）
2. 目标文件（若已存在）+ 同章相邻 `N.M±1`
3. 若有综合稿（如 `A02-*.md`）只作素材，输出仍为**书章体**；同章 `N.M` 已覆盖则综合稿应已归档

## 样板（照抄结构，勿照抄过时链接）

- Bootloader：`01-卷1-平台基础与启动/06-Bootloader 到 Kernel/6.1-*.md`
- Init：`01-卷1-平台基础与启动/07-Init 进程与 init.rc/7.1-*.md`

## 输出检查

- 基线：`android-17.0.0_r1` + `android17-6.18`
- YAML frontmatter + `# N.M` + 一句话开场 + Takeaway
- `// path` + `// commit: android-17.0.0_r1`；对账表
- 链接只用现行 6 卷相对路径
- 完成后提示用户/自行追加 `00-Meta/harness/SERIES_CHANGELOG.md`
