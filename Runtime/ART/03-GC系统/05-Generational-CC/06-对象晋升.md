# 5.6 对象晋升（Promotion）

> **本节回答一个根本问题**：GenCC 中 Young Gen 的对象怎么晋升到 Old Gen？晋升阈值和策略是什么？
>
> **答案**：**年龄阈值 + 自适应策略** —— 对象活过 15 次 Minor GC 就晋升 Old Gen。
>
> **理解本节，就理解了 GenCC 的对象生命周期管理**。

---

## 一、对象年龄的定义

### 5.6.1 对象年龄的存储

```cpp
// art/runtime/obj_ptr.h 的 Object 类
class Object {
    uint32_t age_;  // 对象年龄（每次 Minor GC 存活 +1）
    
    // 检查是否达到晋升阈值
    bool ShouldPromote() {
        return age_ >= kPromotionThreshold;  // 默认 15
    }
};
```

### 5.6.2 对象年龄的更新时机

```
每次 Minor GC：
  1. 标记活对象
  2. 对每个活对象，年龄 +1
  3. 检查年龄是否 >= 阈值
  4. 如果达到阈值 → 晋升到 Old Gen
  5. 如果未达到阈值 → 留在 Young Gen
```

### 5.6.3 晋升阈值配置

```cpp
// art/runtime/gc/collector/concurrent_copying.h
static constexpr uint32_t kPromotionThreshold = 15;

// 可通过 system property 调整
// dalvik.vm.gencc.promotion.threshold = 15
```

---

## 二、晋升的实现

### 5.6.4 晋升的完整流程

```cpp
// art/runtime/gc/collector/concurrent_copying.cc 的 Promote
void ConcurrentCopying::Promote(mirror::Object* obj) {
    // 1. 检查对象年龄
    if (!obj->ShouldPromote()) {
        // 2. 未达阈值 → 复制到 Young Gen 新 Region
        CopyToYoungGen(obj);
        return;
    }
    
    // 3. 达到阈值 → 晋升到 Old Gen
    CopyToOldGen(obj);
    
    // 4. 更新对象状态
    obj->age_ = 0;  // 重置年龄
    obj->SetInOldGen();  // 标记为 Old Gen
}
```

### 5.6.5 CopyToOldGen 实现

```cpp
void ConcurrentCopying::CopyToOldGen(mirror::Object* obj) {
    // 1. 在 Old Gen 中分配新对象
    Region* old_region = AllocOldGenRegion(obj->SizeOf());
    if (old_region == nullptr) {
        // Old Gen 满 → 触发 Major GC
        TriggerMajorGc();
        old_region = AllocOldGenRegion(obj->SizeOf());
    }
    
    // 2. 复制对象内容
    memcpy(old_region->top_, obj, obj->SizeOf());
    mirror::Object* new_obj = (mirror::Object*)old_region->top_;
    old_region->top_ += obj->SizeOf();
    
    // 3. 设置 forwarding address
    obj->SetForwardingAddress(new_obj);
    
    // 4. 更新对象状态
    new_obj->age_ = 0;  // 重置年龄
    new_obj->state_ = kRegionStateOldGen;
}
```

### 5.6.6 CopyToYoungGen 实现

```cpp
void ConcurrentCopying::CopyToYoungGen(mirror::Object* obj) {
    // 1. 在 Young Gen 中分配新对象
    Region* young_region = AllocYoungGenRegion(obj->SizeOf());
    
    // 2. 复制对象内容
    memcpy(young_region->top_, obj, obj->SizeOf());
    mirror::Object* new_obj = (mirror::Object*)young_region->top_;
    young_region->top_ += obj->SizeOf();
    
    // 3. 设置 forwarding address
    obj->SetForwardingAddress(new_obj);
    
    // 4. 更新年龄
    new_obj->age_ = obj->age_ + 1;  // 年龄 +1
    new_obj->state_ = kRegionStateYoungGen;
}
```

---

## 三、晋升策略

### 5.6.7 固定阈值 vs 自适应阈值

| 策略 | 阈值 | 优点 | 缺点 |
|:---|:---|:---|:---|
| **固定阈值** | 15 次 Minor GC | 简单可预测 | 不灵活 |
| **自适应阈值** | 根据 Old Gen 占用率动态调整 | 灵活高效 | 实现复杂 |

### 5.6.8 ART 14+ 的自适应阈值

```cpp
// art/runtime/gc/heap.cc 的 Heap::AdjustPromotionThreshold
void Heap::AdjustPromotionThreshold() {
    // 1. 统计最近的晋升数据
    double promotion_rate = promotion_count_ / uptime_;
    double old_gen_usage = GetOldGenUsage();
    
    // 2. 决策
    if (old_gen_usage > 0.85 && promotion_rate > 100) {
        // Old Gen 满了，晋升太快 → 调高阈值（减少晋升）
        promotion_threshold_ = std::min(promotion_threshold_ + 1, 50);
    } else if (old_gen_usage < 0.5) {
        // Old Gen 有空间 → 调低阈值（提前晋升长寿对象）
        promotion_threshold_ = std::max(promotion_threshold_ - 1, 5);
    }
}
```

### 5.6.9 晋升的批量处理

```cpp
// 批量晋升：一次处理多个对象
void ConcurrentCopying::BatchPromote(std::vector<mirror::Object*>& objects) {
    // 1. 按目标 Region 分组
    std::unordered_map<Region*, std::vector<mirror::Object*>> groups;
    for (mirror::Object* obj : objects) {
        if (obj->ShouldPromote()) {
            Region* target = AllocOldGenRegion(obj->SizeOf());
            groups[target].push_back(obj);
        }
    }
    
    // 2. 批量复制到各 Region
    for (auto& [region, objs] : groups) {
        for (mirror::Object* obj : objs) {
            CopyToOldGenWithRegion(obj, region);
        }
    }
}
```

---

## 四、晋升的工程影响

### 5.6.10 晋升率过高的问题

**问题**：
- Old Gen 快速填满
- 频繁触发 Major GC
- 总体 GC 性能下降

**诊断**：
```bash
# 1. 看晋升速率
adb logcat -s "art" | grep "Promote"
# 输出示例：
# art : Promote 12345 objects from Young to Old  ← 频繁晋升
```

**根因**：
- Young Gen 中的长寿对象过多
- 缓存实现不当（如静态 HashMap）

### 5.6.11 晋升率过低的危害

**问题**：
- Young Gen 反复扫描相同的对象
- Minor GC 效率下降

**诊断**：
```bash
# 1. 看 Minor GC 释放字节数
adb logcat -s "art" | grep "GC freed"
# 输出示例：
# art : GC freed 100KB ← 释放很少 → 对象都在 Young Gen 中"赖着"
```

### 5.6.12 业务代码的晋升优化

**优化 1：长寿对象一次性创建在 Old Gen**

```java
// ✅ 好：单例在 Old Gen
public class AppManager {
    private static final AppManager INSTANCE = new AppManager();
    // INSTANCE 一次性创建，可能直接进入 Old Gen
}

// ❌ 不好：每次调用创建新实例
public AppManager getInstance() {
    return new AppManager();  // 在 Young Gen
}
```

**优化 2：避免 Young Gen 中的长寿对象**

```java
// ✅ 好：用 WeakReference
private static final Map<String, WeakReference<Object>> cache = 
    new HashMap<>();

// ❌ 不好：用 HashMap 强引用
private static final Map<String, Object> cache = new HashMap<>();
// cache 中所有对象都会"晋升"到 Old Gen
```

**优化 3：缓存放在 Old Gen**

```java
// ✅ 好：缓存作为静态字段（类加载时创建）
private static final Map<String, Object> cache = new HashMap<>();

// ❌ 不好：缓存在方法内创建
public void init() {
    Map<String, Object> cache = new HashMap<>();  // 在 Young Gen
    // 每次调用都创建新 cache
}
```

### 5.6.13 晋升的监控

```bash
# 1. 看晋升次数
adb logcat -s "art" | grep "Promote"

# 2. 看 Old Gen 使用率
adb shell dumpsys meminfo <package> | grep -E "Old Gen"

# 3. 看晋升阈值
adb shell getprop dalvik.vm.gencc.promotion.threshold
```

---

## 五、晋升失败的处理

### 5.6.14 Old Gen 满的处理

```cpp
// Old Gen 满时的处理
Region* ConcurrentCopying::AllocOldGenRegion(size_t obj_size) {
    // 1. 尝试从 Old Gen Pool 申请
    Region* region = old_gen_pool_.AllocateRegion();
    if (region != nullptr) {
        return region;
    }
    
    // 2. Old Gen 满 → 触发 Major GC
    LOG(WARNING) << "Old Gen full, triggering Major GC";
    TriggerMajorGc();
    
    // 3. 重试
    region = old_gen_pool_.AllocateRegion();
    if (region != nullptr) {
        return region;
    }
    
    // 4. Major GC 后仍满 → OOM
    return nullptr;
}
```

### 5.6.15 晋升的失败模式

```
晋升失败可能原因：
  1. Old Gen 满 → 触发 Major GC
  2. Major GC 后仍满 → OOM
  3. 连续 Major GC → 频繁卡顿

避免晋升失败：
  - 限制 Old Gen 中的大对象数量
  - 用 ConcurrentHashMap 等线程安全容器
  - 避免内存泄漏
```

---

## 六、晋升的源码索引

### 5.6.16 核心源码路径

```
art/runtime/gc/collector/concurrent_copying.h   # kPromotionThreshold
art/runtime/gc/collector/concurrent_copying.cc  # Promote 实现
art/runtime/gc/heap.cc                         # Heap 晋升相关
art/runtime/obj_ptr.h                           # Object 年龄字段
```

### 5.6.17 关键函数清单

| 函数 | 文件 | 功能 |
|:---|:---|:---|
| `ConcurrentCopying::Promote` | `concurrent_copying.cc` | 晋升主函数 |
| `ConcurrentCopying::CopyToOldGen` | `concurrent_copying.cc` | 复制到 Old Gen |
| `ConcurrentCopying::CopyToYoungGen` | `concurrent_copying.cc` | 复制到 Young Gen |
| `Heap::AdjustPromotionThreshold` | `heap.cc` | 自适应晋升阈值 |

### 5.6.18 关键常量

```cpp
// art/runtime/gc/collector/concurrent_copying.h
static constexpr uint32_t kPromotionThreshold = 15;
```

---

## 七、本节小结

1. **对象年龄**：每次 Minor GC 存活 +1
2. **晋升阈值**：15 次 Minor GC
3. **晋升实现**：从 Young Gen 复制到 Old Gen
4. **晋升策略**：固定阈值 + ART 14+ 自适应
5. **业务影响**：避免 Young Gen 中的长寿对象，减少晋升率

→ **理解对象晋升，就理解了 GenCC 的对象生命周期管理**。

---

## 跨节引用

**本节被以下章节引用**：
- [5.5 Minor/Major GC](./05-Minor-Major-GC.md) —— 晋升触发
- [5.8 实战案例](./08-实战案例.md) —— 分代假说失效的晋升问题
- 07 篇调度 —— 晋升相关的 GC 触发

**本节引用**：
- [5.1 分代假说](./01-分代假说.md) —— 分代假说决定晋升
- [5.2 Young/Old Gen 划分](./02-Young-Old划分.md) —— 两代 Region
