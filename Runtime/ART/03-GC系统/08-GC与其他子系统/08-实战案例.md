# 8.8 实战案例：Hook 框架在 CC GC 下的 3 个崩溃

> **本节是 08 篇的"压轴案例"** —— 用 3 个完整真实案例，把 GC × Hook 的所有知识串起来。

---

## 一、案例 1：Xposed v90 在 Android 8.0 崩溃

### 8.8.1 案例信息

| 维度 | 信息 |
|:---|:---|
| **App** | Xposed 框架用户 |
| **Android 版本** | Android 8.0 (API 26) |
| **Xposed 版本** | v90（适配 ART 7.x） |
| **问题** | 升级 Android 8.0 后，Xposed 启动后立即崩溃 |
| **崩溃信息** | `RuntimeException: ArtMethod entrypoint invalid` |

### 8.8.2 崩溃过程

```bash
# logcat 关键日志
FATAL EXCEPTION: main
Process: de.robv.android.xposed.installer, PID: 12345
java.lang.RuntimeException: ArtMethod entrypoint invalid
  at de.robv.android.xposed.XposedBridge.invokeOriginalMethodNative(Native Method)
  at de.robv.android.xposed.XposedBridge.handleHookedMethod(XposedBridge.java:738)
  at ...
```

### 8.8.3 业务代码

```cpp
// Xposed v90 的 Method Hook 代码（简化）
void* xposed_hook_method(JNIEnv* env, jobject method, void* new_entrypoint) {
    ArtMethod* art_method = reinterpret_cast<ArtMethod*>(env->FromReflectedMethod(method));
    
    // 直接修改 entrypoint，绕过读屏障
    void* old_entrypoint = art_method->entry_point_from_quick_compiled_code_;
    art_method->entry_point_from_quick_compiled_code_ = new_entrypoint;
    
    return old_entrypoint;
}
```

### 8.8.4 问题分析

```
1. Xposed 调用 xposed_hook_method
2. 获取 ArtMethod 指针（可能读到 from-space 地址）
3. 直接修改 entrypoint（绕过读屏障）
4. CC GC 在后台触发
5. ArtMethod 被复制到 to-space
6. from-space 旧 ArtMethod 留下 forwarding address
7. 业务线程调用原方法：
   - 通过栈引用找到 ArtMethod（from-space 旧地址）
   - 读屏障触发，跳转到 to-space 新地址
   - 新地址的 entrypoint 还是原始的（Xposed 修改的是 from-space）
   - 调用原方法 → 但业务期望 Hooked 版本
   - IllegalAccessError
```

### 8.8.5 修复方案

```cpp
// ✅ LSPosed 的修复：用 ReadBarrier
void* lsposed_hook_method(JNIEnv* env, jobject method, void* new_entrypoint) {
    ArtMethod* art_method = reinterpret_cast<ArtMethod*>(env->FromReflectedMethod(method));
    
    // 用读屏障获取最新地址
    art_method = ReadBarrier::BarrierForRoot(art_method);
    
    void* old_entrypoint = art_method->entry_point_from_quick_compiled_code_;
    art_method->entry_point_from_quick_compiled_code_ = new_entrypoint;
    
    return old_entrypoint;
}
```

### 8.8.6 修复效果

```
修复前：
- 启动崩溃率：100%
- 修复后：0%
```

---

## 二、案例 2：Frida 11 在 Android 8.0 偶发崩溃

### 8.8.7 案例信息

| 维度 | 信息 |
|:---|:---|
| **App** | Frida 注入的应用 |
| **Android 版本** | Android 8.0 (API 26) |
| **Frida 版本** | 11.0 |
| **问题** | 注入业务方法后偶发崩溃 |
| **崩溃信息** | `SIGSEGV in artReadBarrier` |

### 8.8.8 崩溃原因

```cpp
// Frida 11 的 ART Method Replacement
void FridaHookMethod(ArtMethod* method, void* new_impl) {
    void* original = method->entry_point_from_quick_compiled_code_;
    method->entry_point_from_quick_compiled_code_ = new_impl;
    // ❌ 绕过读屏障
}

// 崩溃过程：
// 1. Frida 修改 from-space 旧 ArtMethod
// 2. CC GC 复制 ArtMethod 到 to-space
// 3. from-space 旧 ArtMethod 失效
// 4. 业务线程调用旧地址 → SIGSEGV
```

### 8.8.9 修复方案

```cpp
// Frida 12.x 的修复
void FridaHookMethodV2(ArtMethod* method, void* new_impl) {
    // 用读屏障获取最新地址
    method = ReadBarrier::BarrierForRoot(method);
    
    // 修改 entrypoint
    method->entry_point_from_quick_compiled_code_ = new_impl;
}
```

### 8.8.10 修复效果

```
Frida 11：
- 偶发崩溃率：~5%
- 复杂场景崩溃率：~20%

Frida 12.x：
- 偶发崩溃率：0%
- 复杂场景崩溃率：0%
```

---

## 三、案例 3：SandHook 在 Android 9.0 偶发崩溃

### 8.8.11 案例信息

| 维度 | 信息 |
|:---|:---|
| **App** | SandHook App |
| **Android 版本** | Android 9.0 (API 28) |
| **SandHook 版本** | 1.x |
| **问题** | 长时间运行后偶发崩溃 |
| **崩溃信息** | `IllegalAccessError: Method ... was not hooked` |

### 8.8.12 业务代码

```cpp
// SandHook 1.x 的 inline hook
void SandHookInline(ArtMethod* method, void* new_impl) {
    void* original = method->entry_point_from_quick_compiled_code_;
    method->entry_point_from_quick_compiled_code_ = new_impl;  // ❌ 绕过读屏障
}
```

### 8.8.13 修复方案

```cpp
// SandHook 2.x 的修复
void SandHookInlineV2(ArtMethod* method, void* new_impl) {
    method = ReadBarrier::BarrierForRoot(method);
    method->entry_point_from_quick_compiled_code_ = new_impl;
}
```

---

## 四、3 个案例的共同根因

### 8.8.14 共同根因

| 案例 | 共同根因 |
|:---|:---|
| 1-3 | **直接修改 ArtMethod 内存，绕过 ART 读屏障** |

### 8.8.15 Hook 框架的通用工程原则

```
Hook 框架的通用工程原则：

1. 所有 ArtMethod 访问用 ReadBarrier::BarrierForRoot
2. 所有字段修改用 WriteBarrier::WriteField
3. 升级 ART 版本时同步升级 Hook 框架
4. 用 ART 公开 API（不是 internal）
5. 监控 ART Invariant 违反
```

---

## 五、3 个案例的监控

### 8.8.16 Hook 框架的监控

```bash
# 1. ART Invariant 检查
adb logcat -s "art" | grep "Invariant"

# 2. Hook 框架崩溃率
adb logcat -s "AndroidRuntime" | grep "Hook"

# 3. ART Debug 模式
adb shell setprop dalvik.vm.image-dex2oat-flags --debug
```

### 8.8.17 推荐工具

| 工具 | 用途 |
|:---|:---|
| **LSPosed** | Android 8+ 推荐 Xposed 替代 |
| **Frida 14+** | Android 10+ 推荐动态插桩 |
| **Whalebook** | 字节码层 Hook（无 native 风险） |
| **ART Debug** | 不变式检查 |

---

## 六、跨篇引用

**本案例关联的所有章节**：
- [8.4 GC × Hook 框架](./04-GC与Hook框架.md) —— Hook 完整原理
- 04 篇 4.7 —— CC GC 下 Hook 兼容性
- ART 大模块的 `08-Hook与ART` —— Hook 框架的 ART 适配
