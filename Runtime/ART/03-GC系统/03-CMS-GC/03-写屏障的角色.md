# 3.3 并发标记的正确性：写屏障的角色

> **本节回答一个根本问题**：CMS 的 Concurrent Mark 与业务线程并行，怎么保证不漏标？
>
> **答案**：用 **Pre-Write Barrier（Incremental Update）** —— 在每次指针赋值之前，把"被断开引用的对象"重新染灰。
>
> **理解本节，就理解了 CMS 写屏障的核心机制 + 漏标的根本原因**。

---

## 一、漏标的本质

### 3.3.1 什么是漏标

**漏标**（漏活对象）= **本应存活的对象被错误地判定为死亡**。

```cpp
// 时序问题
// T1: GC 扫描 obj_A，将 obj_A 染黑（已完成扫描）
// T2: 业务线程：obj_A.field = null（断开 obj_A → obj_C 的引用）
// T3: 业务线程：obj_B.field = obj_C（建立 obj_B → obj_C 的引用）
//     但 obj_B 也已经被 GC 扫描（染黑）
// T4: GC 完成，obj_C 仍是白色 → 被回收
// T5: 业务线程访问 obj_C → 野指针 / 崩溃
```

### 3.3.2 漏标的充要条件

**Wilson 1992 年证明**：漏标发生的充要条件是 **同时满足**：

1. 业务线程**插入**了一个从**黑色对象**到**白色对象**的新引用
2. 业务线程**删除**了从**灰色对象**到这个白色对象的所有可达路径

```cpp
// 漏标的两个条件（缺一不可）
条件 1: black_obj.field = white_obj  // 黑色引用白色（漏标源头）
条件 2: gray_obj.field = null  // 灰色断开引用（让 white_obj 不可达）
```

### 3.3.3 漏标的具体例子

```
初始状态（GC 已扫描部分）：
  obj_A = 黑色（已扫描）
  obj_B = 灰色（待扫描）
  obj_C = 白色（未扫描）
  
  obj_A.field = obj_C
  obj_B.field = obj_C

GC 继续扫描：
  扫描 obj_B（灰色）→ obj_B 引用 obj_C → obj_C 染灰

漏标场景：
  T1: 业务线程：obj_A.field = null（断开 A → C）
      写屏障：PreWriteBarrier 拦截
      → 把 obj_C（被断开的对象）染灰
  
  T2: 业务线程：obj_B.field = obj_C（建立 B → C）
      → obj_C 已经被染灰，OK

  T3: GC 扫描 obj_B → obj_C 已经在 mark stack → 继续扫描 obj_C
      → obj_C 存活，正确标记

如果不写屏障：
  T1': obj_A.field = null（无拦截）→ obj_C 仍是白色
  T2': obj_B.field = obj_C → obj_C 仍是白色
  T3': GC 扫描 obj_B（已染黑）→ 不再扫描 obj_B 的引用
       obj_C 永远白色 → 漏标 → 回收 → 崩溃
```

### 3.3.4 漏标的后果

**业务线程访问已回收对象**：
```cpp
Object* obj_C = some_list.get(0);
// 此时 obj_C 已被 GC 回收（漏标）
// obj_C 指向已回收的内存 → 野指针
// 访问 obj_C.field → 崩溃 / 内存错误 / 安全漏洞
```

→ **漏标是 GC 最严重的正确性问题之一**。

---

## 二、CMS 的 Pre-Write Barrier（Incremental Update）

### 3.3.5 Pre-Write Barrier 的定义

CMS 用 **Pre-Write Barrier**（增量更新）防止漏标：

```cpp
// 业务代码
obj.field = new_value;

// CMS 实际执行（写屏障插入后）
PreWriteBarrier(obj, field_offset, new_value);  // 写屏障：标记旧值
obj.field = new_value;                          // 真正的赋值
```

### 3.3.6 Pre-Write Barrier 的核心逻辑

```cpp
// art/runtime/gc/collector/mark_sweep.cc 的 MarkSweep::WriteBarrier
void MarkSweep::WriteBarrier(mirror::Object* obj, MemberOffset offset, 
                              mirror::Object* new_value) {
    // 1. 读出旧值
    mirror::Object* old_value = obj->GetFieldObject<mirror::Object>(offset);
    
    // 2. 把旧值重新染灰（Incremental Update）
    if (old_value != nullptr) {
        // 把"被断开引用的对象"重新加入 mark stack
        MarkObject(old_value);
    }
    
    // 3. 真正的赋值由调用方完成
}
```

### 3.3.7 Incremental Update 的逻辑

**Incremental Update** 的精髓：**当黑色对象断开引用时，把被断开的对象"拯救"为灰色**。

```
场景：black_obj.field = null（断开引用）

Pre-Write Barrier 拦截：
1. 读出 old_value（即将被覆盖的引用）
2. 把 old_value 重新染灰（加入 mark stack）

下次 GC 扫描 mark stack：
→ 重新扫描 old_value
→ old_value 的引用都被检查
→ 即使 old_value 已经不被 black_obj 引用，也能被其他对象引用
→ old_value 不漏标
```

### 3.3.8 Pre-Write Barrier 的源码

```cpp
// art/runtime/gc/collector/mark_sweep.cc 的 MarkSweep::MarkObject
void MarkSweep::MarkObject(mirror::Object* obj) {
    // 1. 检查是否已标记
    if (mark_bitmap_->Test(obj)) {
        return;  // 已标记，不重复
    }
    
    // 2. 设置 mark bit
    mark_bitmap_->Set(obj);
    
    // 3. 加入 mark stack（等下一轮扫描）
    mark_stack_->Push(obj);
}
```

### 3.3.9 Pre-Write Barrier 的执行时机

**关键**：Pre-Write Barrier 在赋值 **之前** 执行。

```
时间线：
T1: 业务线程调用 obj.field = new_value
T2: Pre-Write Barrier 执行（读 old_value + 染灰）
T3: 真正的赋值 obj.field = new_value
T4: Post-Write Barrier（CMS 不使用，仅 GenCC 用于 Card Table）
```

### 3.3.10 Pre-Write Barrier 的开销

**每次指针赋值都有开销**：

```cpp
// 简单赋值：obj.field = new_value
// 优化前：
str x1, [x0, #offset]    ; 真正的赋值（1 条指令）

// 优化后（带 Pre-Write Barrier）：
ldr x2, [x0, #offset]    ; 读旧值（1 条指令）
cbz x2, .Lskip            ; 检查旧值是否为 null（1 条指令）
bl artWriteBarrier         ; 调用写屏障（1 条指令）
.Lskip:
str x1, [x0, #offset]    ; 真正的赋值（1 条指令）
```

**开销**：每次指针赋值多 3 条指令（约 5-10ns）。

---

## 三、CMS 写屏障如何防漏标

### 3.3.11 防漏标的关键

**关键洞察**：漏标的两个条件必须 **同时满足**。写屏障只要 **破坏其中一个条件**，漏标就不会发生。

CMS Pre-Write Barrier 破坏的是 **条件 1**：
```
条件 1: black_obj.field = white_obj（黑色引用白色）

CMS 写屏障：在赋值前，把 old_value（被断开的对象）染灰
          → 即使后续赋值 obj.field = new_value
          → 被断开的对象（旧值）已染灰
          → GC 下一轮会扫描它
```

### 3.3.12 完整时序示例

```
初始状态：
  obj_A = 黑色（已扫描完成）
  obj_B = 黑色（已扫描完成）
  obj_C = 白色（未扫描）

业务线程：执行 obj_B.field = obj_C（建立新引用）
         但 obj_A.field 也要断开

CMS 写屏障的介入：
T1: 业务线程：obj_A.field = null（断开 A → C）
    Pre-Write Barrier 拦截：
    → 读 old_value = obj_C
    → 把 obj_C 染灰（加入 mark stack）
    
T2: 真正的赋值 obj_A.field = null

T3: 业务线程：obj_B.field = obj_C（建立 B → C）
    Pre-Write Barrier 拦截：
    → 读 old_value = null（无需染灰）
    → 真正的赋值

T4: GC 完成 Remark
    → 发现 obj_C 在 mark stack
    → 重新扫描 obj_C
    → obj_C 的引用都被检查
    → obj_C 存活，正确标记

结论：漏标被 CMS 写屏障阻止！
```

### 3.3.13 漏标的边界情况

**情况 1：业务线程修改不触发写屏障**

```cpp
// 如果业务线程绕过写屏障
// 例如直接修改内存（Unsafe）
unsafe.putObject(obj, offset, new_value);
// → 没有 Pre-Write Barrier
// → 漏标风险
```

**修复**：ART 11+ 引入了 **JIT 代码校验**，检测绕过写屏障的代码（详见 08 篇）。

**情况 2：写屏障失效**

```cpp
// 极端并发场景
// 写屏障未生效（多线程竞争）
// → 极小概率漏标
// → ART 通过自愈机制减少影响
```

**修复**：ART 通过 **多次 GC** + **STW 校验** 减少漏标概率。

### 3.3.14 CMS 写屏障的局限性

**局限 1：开销大**

每次指针赋值都有 3 条额外指令。循环内多次赋值开销显著。

**局限 2：不能完全防漏标**

极端并发场景下仍可能漏标（虽然概率极小）。

**局限 3：与 JIT 代码集成复杂**

JIT 编译的代码需要手动插入写屏障调用 → 编译复杂度高。

---

## 四、CMS 写屏障 vs 其他 GC 的写屏障

### 3.3.15 三种写屏障对比

| GC | 写屏障类型 | 维护的不变式 | 性能开销 |
|:---|:---|:---|:---|
| **CMS** | Pre-Write Barrier（Incremental Update） | 强三色不变式 | 中（每次指针赋值） |
| **G1** | Pre-Write Barrier（SATB） | 弱三色不变式 | 中 |
| **GenCC** | Post-Write Barrier（Card Table） | 弱三色不变式 | 中 |

### 3.3.16 Incremental Update vs SATB

**Incremental Update（CMS 用）**：
- 关注"被删除的引用"
- 旧值染灰

**SATB（Snapshot At The Beginning，G1 用）**：
- 关注"新建立的引用"
- 记录新值，等 Remark 重新扫描

### 3.3.17 CMS 写屏障的工程选择

CMS 选 Incremental Update 的原因：

| 优势 | 说明 |
|:---|:---|
| **实现简单** | Pre-Write Barrier 只读旧值 |
| **正确性保证** | 强三色不变式的形式化证明 |
| **工程成熟** | ART 5-7 长期使用 |
| **跨平台支持** | x86 / ARM / ARM64 一致 |

---

## 五、CMS 写屏障在编译码中的实现

### 3.3.18 解释器模式的写屏障

```cpp
// art/runtime/interpreter/interpreter.cc
void Interpreter::DoFieldPut(Thread* self, const Instruction* inst, ...) {
    // 1. 解析指令
    // ...
    
    // 2. 调用写屏障
    if (kUseWriteBarrier) {
        WriteBarrier::WriteField(obj, offset, new_value);
    }
    
    // 3. 真正的赋值
    obj->SetFieldObject(offset, new_value);
}
```

### 3.3.19 AOT 编译码的写屏障

```asm
; AArch64 写屏障的实现
; 入口：x0 = obj, x1 = offset, x2 = new_value
pre_write_barrier_entry:
    ; 1. 保存现场
    stp x29, x30, [sp, #-16]!
    mov x29, sp
    
    ; 2. 计算字段地址
    add x3, x0, x1                ; x3 = obj + offset
    
    ; 3. 读旧值
    ldr x4, [x3]                  ; x4 = *obj.offset
    
    ; 4. 检查旧值
    cbz x4, .Lskip                 ; 如果 null，跳过
    
    ; 5. 调用写屏障
    bl artWriteBarrier
    
.Lskip:
    ; 6. 恢复现场
    ldp x29, x30, [sp], #16
    ret
```

### 3.3.20 JIT 编译码的写屏障

ART JIT 编译器会做以下优化：

1. **冗余消除**：连续的写屏障合并
2. **死代码消除**：明显不需要写屏障的代码
3. **内联**：简单写屏障内联到调用点

```cpp
// 优化前（每次循环都触发）
for (int i = 0; i < N; i++) {
    WriteBarrier(&arr[i]);        // 写屏障
    arr[i] = value;
}

// 优化后（循环外判断）
for (int i = 0; i < N; i++) {
    if (i % 100 == 0) {           // 抽样写屏障
        WriteBarrier(&arr[i]);
    }
    arr[i] = value;
}
```

---

## 六、CMS 写屏障的工程坑点

### 3.3.21 坑点 1：Hook 框架绕过写屏障

**场景**：
```cpp
// Hook 框架代码
void* HookMethod(void* method, void* new_entrypoint) {
    ArtMethod* art_method = reinterpret_cast<ArtMethod*>(method);
    // 直接修改 entrypoint，不调用写屏障
    art_method->entry_point_from_quick_compiled_code_ = new_entrypoint;
}

// CMS 在并发标记期间扫描 ArtMethod
// 业务线程修改了 ArtMethod.entrypoint
// 但没有触发写屏障
// → CMS 不记录这个修改
// → ArtMethod 可能被错误回收（如果没被 GC Root 引用）
// → 崩溃
```

**修复**：ART 13+ 引入了 **JIT 代码校验**，部分解决此问题（详见 08 篇）。

### 3.3.22 坑点 2：JNI 直接修改对象字段

**场景**：
```cpp
// JNI 代码直接修改字段
void JNI修改字段(JNIEnv* env, jobject obj, jstring new_value) {
    // 直接写字段内存
    char* field_addr = reinterpret_cast<char*>(obj) + offset;
    *reinterpret_cast<jstring*>(field_addr) = new_value;
    // ❌ 没调用写屏障！
}
```

**修复**：
```cpp
// 正确做法
void JNI修改字段(JNIEnv* env, jobject obj, jstring new_value) {
    // 调用 JNI 接口，内部自动调用写屏障
    env->SetObjectField(obj, fieldID, new_value);
}
```

### 3.3.23 坑点 3：反射 Unsafe 操作

**场景**：
```java
// Unsafe 操作不调用写屏障
Object value = unsafe.getObject(obj, offset);
unsafe.putObject(obj, offset, new_value);
// ❌ 风险：CMS 并发标记期间可能漏标
```

**修复**：用 `Field.set()` 替代 `Unsafe.putObject()`。

---

## 七、CMS 写屏障的调试与监控

### 3.3.24 ART 调试模式

```bash
# 启用 ART 调试日志
adb shell setprop dalvik.vm.image-dex2oat-flags --debug

# 看写屏障执行次数
adb logcat -s "art" | grep "WriteBarrier"
# 输出示例：
# art : WriteBarrier called 12345 times in 100ms
```

### 3.3.25 Perfetto 追踪写屏障

```bash
# Perfetto trace 中追踪写屏障
# 在 atrace 的 ART 类别中包含 write_barrier 事件

adb shell perfetto --out /data/local/tmp/trace.proto \
  -t 10s sched freq idle am wm gfx view binder_driver hal dalvik
```

### 3.3.26 写屏障的性能监控

| 指标 | 含义 | 监控方式 | 告警阈值 |
|:---|:---|:---|:---|
| **写屏障次数/秒** | 每秒调用次数 | ART Trace | > 100 万/秒 异常 |
| **写屏障总耗时** | 写屏障累计耗时 | ART Trace | > 10ms/秒 异常 |
| **写屏障占比** | 写屏障耗时 / CPU 时间 | ART Trace | > 5% 异常 |

---

## 八、CMS 写屏障的源码索引

### 3.3.27 核心源码路径

```
art/runtime/gc/collector/mark_sweep.h           # MarkSweep 类（含 WriteBarrier）
art/runtime/gc/collector/mark_sweep.cc          # WriteBarrier 实现
art/runtime/write_barrier.h                     # 写屏障抽象层
art/runtime/write_barrier.cc                    # 写屏障通用实现
art/runtime/arch/arm64/quick_entrypoints_arm64.S # AArch64 写屏障机器码
art/runtime/arch/x86/quick_entrypoints_x86.S     # x86 写屏障机器码
art/runtime/arch/x86_64/quick_entrypoints_x86_64.S # x86_64 写屏障机器码
art/runtime/arch/arm/quick_entrypoints_arm.S     # ARM 写屏障机器码
art/runtime/jit/jit_code_cache.cc               # JIT 模式写屏障
```

### 3.3.28 关键函数清单

| 函数 | 文件 | 功能 |
|:---|:---|:---|
| `MarkSweep::WriteBarrier` | `mark_sweep.cc` | CMS 写屏障入口 |
| `MarkSweep::MarkObject` | `mark_sweep.cc` | 标记对象 |
| `MarkSweep::MarkObjectParallel` | `mark_sweep.cc` | 并发标记对象（CAS） |
| `WriteBarrier::WriteField` | `write_barrier.cc` | 写屏障通用入口 |
| `WriteBarrier::WriteBarrierField` | `write_barrier.cc` | 字段写屏障 |

### 3.3.29 写屏障的入口注册

```cpp
// art/runtime/gc/heap.cc 的 Heap 初始化
Heap::Heap(...) {
    // 注册写屏障
    pre_write_barrier_ = [this](mirror::Object* obj, MemberOffset offset, mirror::Object* new_value) {
        if (kUseCMS) {
            mark_sweep_->WriteBarrier(obj, offset, new_value);
        }
    };
}
```

---

## 九、本节小结

1. **漏标 = 本应存活的对象被错误回收**
2. **CMS 用 Pre-Write Barrier（Incremental Update）防漏标**：把被断开的引用对象重新染灰
3. **写屏障破坏漏标的"条件 1"**：黑色对象断开引用时，旧值染灰
4. **CMS 写屏障的开销**：每次指针赋值多 3 条指令
5. **CMS 写屏障的局限性**：Hook / JNI / Unsafe 可能绕过

→ **理解 CMS 写屏障，就理解了"为什么 CMS 维护强三色不变式"**。

---

## 跨节引用

**本节被以下章节引用**：
- [3.4 Sweep 的实现](./04-Sweep的实现.md) —— Sweep 与写屏障的协同
- [3.5 STW 时间分析](./05-STW时间分析.md) —— dirty 对象数影响 Remark STW
- 04 篇 CC GC —— CMS 写屏障 vs CC 读屏障对比

**本节引用**：
- [01 篇 1.2 三色不变式](../01-基础理论/02-三色标记不变式.md) —— 强三色不变式的形式化
- [01 篇 1.3 写屏障机制](../01-基础理论/03-写屏障机制.md) —— 写屏障的通用原理
- [3.2 标记-清除的 4 阶段](./02-标记-清除的4阶段.md) —— 写屏障在 4 阶段中的作用
