# ⚠️ v1 旧稿标记

> **本篇状态**：v1 后期已按 v4 规范写（含 A/B/D 附录），但 ART 17 硬变化未覆盖
>
> - **本篇基线**：AOSP `android-14.0.0_r1`（API 34）+ Linux `android14-5.10/5.15`（**v1 时代基线**）
> - **v2 新基线**：AOSP `android-17.0.0_r1`（API 37）+ Linux `android17-6.18`
> - **ART 17 硬变化未覆盖**：分代 GC 强化 / 无锁 MessageQueue（API 37+）/ static final 不可变 / AppFunctions / AI Agent OS
>
> **v2 替代/补充篇**（已按 v4 规范 + AOSP 17 + 6.18 写完）：
>
> [10-ART17分代GC强化专章 v2](10-ART17分代GC强化专章-v2.md) · 分代 GC 强化（频繁低耗年轻代回收 + 软阈值 + 端侧 LLM 友好）· [README-ART系列-v2](../../README-ART系列-v2.md) 含 9 子模块 v2 全部链接
>
> **建议**：本篇读完后**必须结合 v2 篇一起看** —— v1 讲基础机制，v2 讲新基线变化。
>
> **当前文件名**：`09-实战案例.md`
> **标记时间**：2026-07-17（v2 全系列成稿后批量标）
>
> ---

# 6.9 实战案例：finalize() 链式阻塞的完整分析

> **本节是 06 篇的"压轴案例"** —— 用多个真实案例，把 Finalizer、Cleaner、Reference 体系的所有知识串起来。
>
> **理解本节，就掌握了"如何治理 finalize() 阻塞"的完整方法论**。

---

## 一、案例背景

### 6.9.1 案例总览

| 案例 | App | 问题 | 根因 |
|:---|:---|:---|:---|
| 1 | 数据库 App | OOM 崩溃 | Cursor finalize() 链式阻塞 |
| 2 | 图片 App | native 内存泄漏 | Bitmap finalize() 阻塞 |
| 3 | 系统服务 | Watchdog 警告 | Theme finalize() 卡死 |
| 4 | DirectByteBuffer | native 内存泄漏 | 没有用 Cleaner |

---

## 二、案例 1：Cursor finalize() 链式阻塞

### 6.9.2 案例信息

| 维度 | 信息 |
|:---|:---|
| **App** | 某数据库 App |
| **Android 版本** | Android 12 (API 31) |
| **问题** | 运行 30 分钟后 OOM 崩溃 |
| **崩溃信息** | `OutOfMemoryError: Failed to allocate` |

### 6.9.3 业务代码

```java
public class DatabaseHelper {
    private SQLiteDatabase database;
    
    public List<Data> queryData() {
        Cursor cursor = database.rawQuery("SELECT * FROM data", null);
        // 忘记 close cursor
        List<Data> result = parseCursor(cursor);
        return result;
    }
}
```

### 6.9.4 问题分析

```
1. 每次 queryData() 都创建 Cursor
2. Cursor 在 finalize() 中关闭
3. 如果 Cursor 有未完成的查询 → finalize() 阻塞
4. 多个 Cursor 阻塞 → finalize() 队列堆积
5. FinalizerWatchdogDaemon 警告
6. 队列中的 Cursor 占内存 → OOM
```

### 6.9.5 崩溃过程

```bash
# logcat 关键日志
FATAL EXCEPTION: main
java.lang.OutOfMemoryError: Failed to allocate a 24 byte allocation
  at android.database.CursorWindow.nativeGetCursorPosition(Native Method)
  at android.database.CursorWindow.getPosition(CursorWindow.java:519)
  at android.database.AbstractCursor.getPosition(AbstractCursor.java:42)
  at ...

W art : Finalizer watch dog timed out: 15000ms, count=1
W art : Finalizer watch dog timed out: 30000ms, count=1
W art : Finalizer watch dog timed out: 45000ms, count=1
```

### 6.9.6 修复方案

**方案 1：try-with-resources 关闭 Cursor**

```java
public List<Data> queryData() {
    try (Cursor cursor = database.rawQuery("SELECT * FROM data", null)) {
        return parseCursor(cursor);
    }
    // close() 自动调用，不会进入 finalize
}
```

**方案 2：手动关闭 Cursor**

```java
public List<Data> queryData() {
    Cursor cursor = null;
    try {
        cursor = database.rawQuery("SELECT * FROM data", null);
        return parseCursor(cursor);
    } finally {
        if (cursor != null) {
            cursor.close();  // 显式关闭
        }
    }
}
```

**方案 3：重构代码，避免 Cursor**

```java
public List<Data> queryData() {
    // 用 Room/Realm 等 ORM 框架，无需手动管理 Cursor
    return database.dataDao().queryAll();
}
```

### 6.9.7 修复效果

```
修复前：
- Cursor 数量：~5000（30 分钟内）
- FinalizerWatchdogDaemon 警告：~50 次
- OOM 崩溃率：~80%

修复后：
- Cursor 数量：0（try-with-resources 立即关闭）
- FinalizerWatchdogDaemon 警告：0 次
- OOM 崩溃率：0%
```

---

## 三、案例 2：Bitmap finalize() 阻塞

### 6.9.8 案例信息

| 维度 | 信息 |
|:---|:---|
| **App** | 某图片编辑器 |
| **Android 版本** | Android 13 (API 33) |
| **问题** | native 内存泄漏，最终 OOM |
| **现象** | dumpsys meminfo 显示 native 内存持续增长 |

### 6.9.9 业务代码

```java
public class ImageEditor {
    private List<Bitmap> undoStack = new ArrayList<>();
    
    public void addUndo(Bitmap bitmap) {
        undoStack.add(bitmap);
        // 忘记 recycle
    }
    
    public void undo() {
        if (!undoStack.isEmpty()) {
            Bitmap bitmap = undoStack.remove(undoStack.size() - 1);
            // 忘记 recycle
            applyBitmap(bitmap);
        }
    }
}
```

### 6.9.10 问题分析

```
1. Bitmap 在 finalize() 中释放 native 内存
2. 业务代码不 recycle → 等 GC finalize 释放
3. 大量 Bitmap 堆积 → finalize 队列长
4. 每个 Bitmap finalize 释放 native 内存
5. 但 finalize 处理速度 < Bitmap 创建速度
6. → native 内存持续增长 → OOM
```

### 6.9.11 监控数据

```bash
$ adb shell dumpsys meminfo com.example.imageeditor

# 关键数据
  Native Heap      123456   100000    23456      500  150000   204800   123456    81344
  Dalvik Heap      45678    40000     5678      100    51234    98304    45678    52626
  Graphics         23456    20000     3456      100    28000
  TOTAL            234567   200000    34567      600  280000

# Native Heap Alloc = 123 MB（异常高）
# Graphics = 23 MB（Bitmap native 占用）
```

### 6.9.12 修复方案

**方案 1：及时 recycle() Bitmap**

```java
public class ImageEditor {
    private List<Bitmap> undoStack = new ArrayList<>();
    
    public void undo() {
        if (!undoStack.isEmpty()) {
            Bitmap bitmap = undoStack.remove(undoStack.size() - 1);
            try {
                applyBitmap(bitmap);
            } finally {
                if (bitmap != null && !bitmap.isRecycled()) {
                    bitmap.recycle();  // 立即释放 native
                }
            }
        }
    }
}
```

**方案 2：用 Glide / Fresco 自动管理**

```java
// Glide 自动 recycle Bitmap
Glide.with(context)
    .load(url)
    .into(imageView);

// Glide 内部使用 BitmapPool + LRU
```

**方案 3：用 inBitmap 复用**

```java
public Bitmap decodeBitmap(String path) {
    BitmapFactory.Options options = new BitmapFactory.Options();
    options.inBitmap = reusableBitmap;  // 复用 native 内存
    options.inMutable = true;
    return BitmapFactory.decodeFile(path, options);
}
```

### 6.9.13 修复效果

```
修复前：
- Native 内存：~200 MB
- Bitmap 数量：~500
- finalize() 队列：~200 个等待

修复后：
- Native 内存：~50 MB（4x 下降）
- Bitmap 数量：~30（LRU 限制）
- finalize() 队列：~10 个等待
```

---

## 四、案例 3：Theme finalize() 卡死

### 6.9.14 案例信息

| 维度 | 信息 |
|:---|:---|
| **App** | 系统服务 |
| **Android 版本** | Android 12 (API 31) |
| **问题** | FinalizerWatchdogDaemon 警告频发 |
| **现象** | `Finalizer watch dog timed out: 15000ms` |

### 6.9.15 业务代码

```java
public class Theme {
    private long nativeThemeHandle;
    
    @Override
    protected void finalize() throws Throwable {
        super.finalize();
        // native 释放
        if (nativeThemeHandle != 0) {
            nativeDestroy(nativeThemeHandle);
            nativeThemeHandle = 0;
        }
    }
}
```

### 6.9.16 问题分析

```
1. Theme 在 finalize() 中释放 native 资源
2. 如果 native 资源被占用 → finalize() 阻塞
3. 例如：Theme 的 native 资源还在 GPU 渲染中
4. GPU 渲染线程持有 native 资源
5. finalize() 等待 GPU 释放 → 阻塞
6. 多个 Theme finalize() 排队 → 系统卡顿
```

### 6.9.17 监控数据

```bash
$ adb logcat -s "art" | grep "Finalizer"
W art : Finalizer watch dog timed out: 15000ms, count=1
W art : Finalizer watch dog timed out: 30000ms, count=1
```

### 6.9.18 修复方案

**方案 1：用 Cleaner 替代 finalize()**

```java
public class Theme {
    private final Cleaner cleaner;
    
    public Theme() {
        this.nativThemeHandle = nativeCreate();
        this.cleaner = Cleaner.create(this, () -> {
            // 异步释放 native 资源
            if (nativeThemeHandle != 0) {
                nativeDestroy(nativeThemeHandle);
                nativeThemeHandle = 0;
            }
        });
    }
    
    // 主动释放（推荐）
    public void close() {
        cleaner.clean();
    }
}
```

**方案 2：主动关闭 Theme**

```java
// 业务代码
Theme theme = new Theme();
// 使用 theme
theme.close();  // 主动关闭，避免依赖 finalize
```

### 6.9.19 修复效果

```
修复前：
- finalize() 超时警告：~30 次/小时
- finalize() 队列：~20 个等待
- 系统卡顿：明显

修复后：
- finalize() 超时警告：0 次
- finalize() 队列：0 个
- 系统卡顿：无
```

---

## 五、案例 4：DirectByteBuffer 内存泄漏

### 6.9.20 案例信息

| 维度 | 信息 |
|:---|:---|
| **App** | 某图片处理 App |
| **Android 版本** | Android 13 (API 33) |
| **问题** | native 内存持续增长 |
| **现象** | OOM 但 Java 堆使用率不高 |

### 6.9.21 业务代码

```java
public class ImageProcessor {
    public Bitmap processImage(Bitmap source) {
        // 分配 native 内存
        ByteBuffer inputBuf = ByteBuffer.allocateDirect(source.getByteCount());
        ByteBuffer outputBuf = ByteBuffer.allocateDirect(source.getByteCount());
        
        // 业务处理
        // ... GPU 处理 ...
        
        // 忘记释放 buf
        return resultBitmap;
    }
}
```

### 6.9.22 问题分析

```
1. DirectByteBuffer 用 Cleaner 释放 native 内存
2. 但业务代码持有 buf 的引用（如成员变量）
3. buf 不被 GC → Cleaner 不触发
4. native 内存持续增长 → OOM
```

### 6.9.23 监控数据

```bash
$ adb shell dumpsys meminfo com.example.imageprocessor

  Native Heap      234567   200000    34567      500  280000
  Dalvik Heap      45678    40000     5678      100    51234
  TOTAL            280245   240000    40245      600  331290

# Native 内存 234 MB（异常高）
# 但 Java 堆只有 45 MB（不高）
# → DirectByteBuffer 泄漏
```

### 6.9.24 修复方案

**方案 1：及时释放 ByteBuffer**

```java
public Bitmap processImage(Bitmap source) {
    ByteBuffer inputBuf = null;
    ByteBuffer outputBuf = null;
    try {
        inputBuf = ByteBuffer.allocateDirect(source.getByteCount());
        outputBuf = ByteBuffer.allocateDirect(source.getByteCount());
        
        // 业务处理
        // ...
        
        return resultBitmap;
    } finally {
        // 主动释放（如果业务层支持）
        if (inputBuf != null) {
            ((DirectBuffer) inputBuf).cleaner().clean();
        }
        if (outputBuf != null) {
            ((DirectBuffer) outputBuf).cleaner().clean();
        }
    }
}
```

**方案 2：用对象池复用 ByteBuffer**

```java
public class ByteBufferPool {
    private final ConcurrentLinkedQueue<ByteBuffer> pool = new ConcurrentLinkedQueue<>();
    
    public ByteBuffer acquire(int size) {
        ByteBuffer buf = pool.poll();
        if (buf == null || buf.capacity() < size) {
            buf = ByteBuffer.allocateDirect(size);
        } else {
            buf.clear();
        }
        return buf;
    }
    
    public void release(ByteBuffer buf) {
        if (buf != null) {
            pool.offer(buf);
        }
    }
}
```

**方案 3：监控 DirectByteBuffer 数量**

```java
public class DirectByteBufferMonitor {
    @Scheduled(fixedRate = 30000)
    public void monitor() {
        // 计算 DirectByteBuffer 数量
        int count = countDirectByteBuffers();
        // 上报到 APM
        apmClient.report("directbuffer.count", count);
        
        if (count > 1000) {
            apmClient.alert("directbuffer.high", "DirectByteBuffer > 1000");
        }
    }
}
```

### 6.9.25 修复效果

```
修复前：
- DirectByteBuffer 数量：~2000
- Native 内存：~200 MB

修复后（用对象池）：
- DirectByteBuffer 数量：~20（复用）
- Native 内存：~30 MB（6x 下降）
```

---

## 六、案例总结与治理方案

### 6.9.26 4 个案例的共同根因

| 案例 | 共同根因 |
|:---|:---|
| 1-4 | **依赖 finalize() / Cleaner 自动释放，业务层不及时显式释放** |

### 6.9.27 治理方案：完全禁止 finalize()

**原则 1：用 AutoCloseable 替代 finalize()**

```java
// ❌ 旧风格
public class Resource {
    @Override
    protected void finalize() throws Throwable {
        close();
    }
}

// ✅ 新风格
public class Resource implements AutoCloseable {
    @Override
    public void close() {
        // 显式释放
    }
}

// 使用
try (Resource res = new Resource()) {
    // 业务逻辑
}
```

**原则 2：用 Cleaner 替代 finalize()（用于 native 资源）**

```java
public class Resource {
    private final Cleaner cleaner;
    
    public Resource() {
        this.cleaner = Cleaner.create(this, () -> {
            // 异步释放 native 资源
        });
    }
}
```

**原则 3：主动关闭 vs 被动清理**

```java
// 主动关闭（推荐）
try (Resource res = new Resource()) {
    // 业务逻辑
}

// 被动清理（兜底）
// Cleaner 在 GC 时自动清理（防止业务层忘记）
```

### 6.9.28 监控指标

```java
public class FinalizerMonitor {
    // 监控 finalize() 超时
    @Scheduled(fixedRate = 60000)
    public void monitorFinalizerTimeouts() {
        // 1. 检查 FinalizerWatchdogDaemon 警告
        // 2. 检查 finalize() 队列长度
        // 3. 上报到 APM
    }
    
    // 监控 finalize() 业务代码
    @Scheduled(fixedRate = 300000)
    public void monitorFinalizerUsage() {
        // 1. 扫描业务代码
        // 2. 找出重写 finalize() 的类
        // 3. 上报到 APM
    }
}
```

### 6.9.29 治理 checklist

```
□ 1. 业务代码不使用 finalize()（除了特殊场景）
□ 2. native 资源用 Cleaner 释放
□ 3. Java 资源用 AutoCloseable + try-with-resources
□ 4. 监控 FinalizerWatchdogDaemon 警告
□ 5. 监控 finalize() 队列长度
□ 6. APM 告警：finalize() 超过 10 秒
□ 7. 定期扫描业务代码中的 finalize() 用法
□ 8. 培训开发人员：不使用 finalize()
```

---

## 七、跨篇引用

**本案例关联的所有章节**：
- [6.4 FinalReference](./04-FinalReference.md) —— finalize() 的本质
- [6.7 FinalizerDaemon 源码](./07-FinalizerDaemon源码.md) —— FinalizerDaemon 实现
- [6.8 FinalizerWatchdogDaemon 源码](./08-FinalizerWatchdog源码.md) —— 10 秒超时
- 09 篇诊断 —— 完整的 GC 诊断流程

**本案例的延伸**：
- ART 大模块的 `04-JNI` —— JNI 资源管理
- Android_Framework 的 `Memory_Management` —— 进程内存治理
