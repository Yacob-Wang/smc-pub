# 技术系列文章写作 Prompting 指南 v3

> 本文件是一套经过多系列实战验证的 AI 提示词模板，用于指导 AI 生成**有深度、有广度、有源码依据、有工程基线、可验证**的技术系列文章。
> 适用于任何技术模块（内核子系统、Framework 服务、Native 库、协议层等）的系列化学习输出。
>
> **v3 增量**：基于 v2 在 Input / ART / Window / MM_v2 / Binder / IO / Socket / Interrupt 等系列的实际写作经验，补齐 v2 的工程化盲区：
> - 引入**多版本基线矩阵**（AOSP + Linux Kernel），适配 epoll / 信号 / 调度等内核强相关内容
> - 反面教材升级为**反例库（带反向索引）**，支持跨系列复用
> - 新增**图表密度规则**、**工程默认值基线段**、**量化数据自检表**、**源码路径对账表**
> - 新增**本篇定位声明**段，从源头减少跨篇重复
> - 强化案例**可验证性要求**（含 dump 片段、版本号、复现步骤）
> - 质量清单从 21 项扩到 26 项

---

## 一、核心原则（写在最前面，每次对话都要强调）

```
你写的不是科普文，不是面试八股，而是给【资深架构师】看的技术深度文章。
必须满足以下硬性要求：

1. 言必有据，码必有源：每个技术论点必须标注对应的源码文件路径，关键流程必须贴核心源码片段。
   每篇文章末尾必须给出"源码路径对账表"，逐条标注【已校对 / 待确认】。
2. 自顶向下：每个章节先讲"是什么、为什么需要它"，建立上下文后再深入源码细节。禁止上来就贴代码。
3. 关联实战：每个知识点必须关联到真实的工程问题（Crash/ANR/OOM/性能退化等），说清楚"这个机制会在什么场景下出问题"。
4. 实战案例收尾：每篇文章末尾必须有 1-2 个完整的线上问题排查案例（现象→分析→根因→修复），案例必须可验证（含 dump/日志片段、版本号、复现步骤）。
5. 内容量级：每篇文章不少于 300 行（约 8000-15000 字），确保广度和深度兼具。
6. 版本基线（多版本矩阵）：
   - 应用层 / Framework：以 Android 14（AOSP android-14.0.0_r1）为主线
   - Linux 内核：以 android14-5.10/android14-5.15/android15-6.1/android15-6.6 为基线
   - 涉及 GKI 厂商分支差异时，标注"厂商 GKI"或具体 vendor tag
   - 涉及历史演进或多版本差异时，必须标注版本范围
7. 工程基线：涉及可调参数（timeout、buffer size、maxevents、优先级等）时，必须给出"工程默认值"与"选用准则"，说清楚"线上我应该用啥、为啥"。
```

---

## 二、系列文章的设计流程

### 第一步：定义角色与目标

```
我的角色是 Android 稳定性高级工程师。

我要系统学习【填入模块名称，如：Binder / SurfaceFlinger / Linux 调度器 / epoll】。

学习目标不是"了解原理"，而是：
- 能在遇到线上问题时，快速定位到该模块的对应子系统和源码文件
- 能理解问题的根因机制
- 能给出治理方案或监控手段
```

### 第二步：让 AI 先输出大纲（不要直接写文章）

```
请按照以下架构师思维链，为【模块名称】设计一套系列文章的大纲：

架构师看一个系统的逻辑链：
  它是什么？解决什么问题？（定位）
      ↓
  它在系统中处于什么位置？和谁协作？（边界与交互）
      ↓
  它内部是怎么运转的？（核心机制）
      ↓
  它会在什么地方出问题？（风险地图）
      ↓
  出了问题我怎么查？怎么防？（诊断与治理）

大纲要求：
1. 分为 3-5 个篇章（全局观 → 核心机制 → 跨模块交互 → 实战/治理）
2. 每篇文章列出具体章节，每个章节说清楚：
   - 讲什么内容
   - 对应哪个源码文件/目录（含内核版本基线）
   - 关联什么工程问题
3. 用表格形式呈现每篇文章的章节规划
4. 明确标注与其他系列（如已有的 Input / ART / MM_v2 / IO / Socket / Syscalls）的交叉引用点
   - 引用矩阵格式：| 本篇章节 | 引用系列 | 引用文章 | 引用原因 |
5. 如果该模块与其他模块有深度交互（如 WMS 与 Input 的焦点管理），
   应为交互部分单独设置专题篇章，而非分散在多篇中一笔带过
6. 明确标注"本篇定位"：在本系列中属于什么角色（全局观/核心机制/横切专题/风险/治理），
   强依赖哪几篇已发布文章
```

### 第三步：生成系列 README

```
大纲审核通过后，请生成一份系列导读文档（README-【模块名】系列.md），包含：

1. 为什么要写这个系列
   - 该模块在稳定性领域的重要性（用数据说话，如"占线上 ANR 的 X%"）
   - 对稳定性工程师的核心价值

2. 系列设计思路
   - 用架构师思维链的具体展开形式，展示从定位到治理的逻辑链
   - 给出"依赖关系图"：本系列各篇之间的依赖方向
   - 给出"跨系列引用矩阵"：本系列与已有系列哪几篇有双向引用

3. 每篇文章的章节规划表格
   - 格式：| 章节 | 内容 | 核心源码路径 | 内核版本基线 | 稳定性关联 |
   - 必须有"本篇定位"列

4. 与其他系列的交叉引用
   - 列出与已有系列的关联文章和互相引用关系
   - 列出与已有系列内容的"边界声明"（什么不重复讲、什么一笔带过）

5. 阅读建议
   - 时间有限优先阅读哪几篇
   - 系统学习的推荐顺序
   - 每篇文章的设计逻辑（背景 → 架构 → 源码 → 风险 → 案例）

6. 质量基线（可选，仅横切型系列需要）
   - 若本系列涉及可调参数，给出"工程默认值表"：参数 / 典型默认 / 选用准则 / 踩坑提醒
```

### 第四步：逐篇撰写（使用一站式模板）

大纲和 README 审核通过后，使用下方**一站式写作模板**逐篇撰写。每次只需替换 `【】` 中的内容。

---

## 三、一站式写作模板

以下是一个可直接复制使用的完整提示词模板，整合了角色设定、写作约束、章节结构和禁止事项。

````
# 本篇定位（强制开头段，先写它再写正文）
- 本篇系列角色：【全局观 / 核心机制 / 横切专题 / 风险地图 / 诊断治理】
- 强依赖：必须先读【文章 A】的【章节 X】、【文章 B】的【章节 Y】
- 承接自：上一篇【文章 C】已覆盖【简述】，本篇不重复展开
- 衔接去：下一篇【文章 D】将覆盖【简述】，本篇末尾会预告
- 不重复内容：与本系列已发布/已规划文章中【哪些内容】的边界声明（一句话）

# 角色设定
我是一名【Android 稳定性架构师】，正在系统学习【模块名称】。
本篇是系列的第【N】篇，主题是【具体主题】。

# 上下文
- 上一篇是【上一篇标题】，已覆盖了【简述上一篇核心内容】
- 下一篇是【下一篇标题】，将覆盖【简述下一篇核心内容】
- 本系列的 README 见【README 文件名】

# 写作标准

## 硬性要求
1. 目标读者：资深架构师，不是初学者。不需要解释基础概念（如什么是线程、什么是指针），
   但需要解释该模块特有的术语和机制。
2. 每个章节必须先讲"这个东西是什么、为什么需要它、解决什么问题"，然后再深入源码。
3. 涉及源码时：
   - 必须标注源码文件路径（如 `frameworks/native/services/inputflinger/dispatcher/InputDispatcher.cpp`
     或 `fs/eventpoll.c`），**并标注内核版本基线**
   - 只贴核心逻辑，不要全贴
   - 贴代码前先用自然语言解释这段代码要干什么
   - 贴代码后紧跟"稳定性架构师视角"分析
   - 如果不确定某个文件路径是否准确，用 `// 路径待确认` 标注，并在"源码路径对账表"中标记【待确认】
4. 每个技术点必须关联到实际的工程问题（Crash、ANR、OOM、JE、NE、Watchdog、性能退化、兼容性问题），
   说清楚"它会在什么场景下咬你一口"。
5. 涉及量化描述时，必须给出数量级（"ms 级""秒级""占比 X%"），禁止使用"大约""通常"等模糊用词。
   量化依据须填入"量化数据自检表"。
6. 源码版本基线：
   - 应用层 / Framework：以 Android 14（AOSP android-14.0.0_r1）为主线
   - Linux 内核：以 android14-5.10 / android14-5.15 / android15-6.1 / android15-6.6 为基线
   - 涉及厂商 GKI 分支差异时，标注"厂商 GKI"或具体 vendor tag
   - 涉及历史演进或多版本差异时，必须标注版本范围
7. 工程基线要求：涉及可调参数（timeout、buffer size、maxevents、nice/ionice 等级等）时，
   必须给出"工程默认值"与"选用准则"，说清楚"线上我应该用啥、为啥"。
8. 文章长度不少于 300 行（约 8000-15000 字）。

## 章节结构
请严格按照以下结构组织内容：

```
┌─ 背景与定义（是什么、为什么需要它、解决什么问题）
│     不要假设读者已经知道，先用 1-2 段建立上下文
│
├─ 架构与交互（在系统中的位置、上下游依赖、数据流向）
│     用 ASCII Art 画架构图或层级图，说清楚边界
│     标注本篇重点在全局架构中的位置
│
├─ 核心机制与源码（关键数据结构、核心流程、源码走读）
│     每段源码必须标注 AOSP/内核 源码路径 + 内核版本基线
│     贴代码前先用自然语言解释这段代码要干什么
│     代码后紧跟"稳定性架构师视角"分析
│
├─ 风险地图（这个机制会在哪些场景下出问题）
│     列出具体的异常类型、日志特征、触发条件
│     用表格形式呈现"问题类型 / 日志关键字 / 排查入口"
│
├─ 实战案例（1-2 个线上问题的完整排查过程）
│     格式：现象 → 分析思路 → 根因 → 修复方案
│     案例必须可验证：
│       · 现象：贴关键 logcat / dmesg / systrace 片段
│       · 环境：Android 版本 / 内核版本 / 设备 / 复现步骤
│       · 修复：贴修复 commit 或配置 diff
│     案例基于通用模式构造时标注"典型模式"，
│     基于公开 bug report 时标注来源
│
├─ 总结（3-5 条架构师视角的关键 Takeaway）
│     浓缩全篇为"排查时需要记住的几件事"
│     给出该主题的排查路径速查
│
├─ 附录 A：核心源码路径索引（表格）
│     格式：| 文件名 | 完整路径 | 内核版本基线 | 说明 |
│     集中列出本篇涉及的所有源码文件，方便检索
│
├─ 附录 B：源码路径对账表（强制）
│     格式：| 序号 | 文章中出现的路径 | 已校对/待确认 | 校对来源 |
│     用于在 AOSP Code Search / LXR / Elixir 上逐条核对，杜绝路径幻觉
│
├─ 附录 C：量化数据自检表（强制）
│     格式：| 序号 | 量化描述 | 数量级 | 依据来源（源码常量 / 线上统计 / 文档）|
│
├─ 附录 D：工程基线表（若涉及可调参数）
│     格式：| 参数 | 典型默认 | 选用准则 | 踩坑提醒 |
│
└─ 篇尾衔接
      用 1-2 句说明下一篇将深入什么内容
      格式：下一篇 [文章标题](文件名.md) 将深入...
```

## 图表格式
- 架构图/层级图/流程图：统一使用 ASCII Art（纯文本方框图），
  确保在任何 Markdown 渲染器中正常显示，不依赖外部渲染工具
- 对比信息：使用 Markdown 表格
- 时序说明：使用缩进文本 + 箭头符号

## 图表密度规则
- **平均每 1500 字 1 张图**
- **单篇 4-6 张图（不含源码中的小型 ASCII 示意）**
- 上限 6 张：超过说明信息密度没跟上
- 下限 4 张：少于说明架构/流程讲得不到位
- 表格不计入图数

## 跨模块引用规范
- 当涉及本系列其他篇章已覆盖的内容时，用 `[文章标题](文件名.md)` 引用，不重复展开
- 当涉及其他系列已覆盖的内容时，用 `[系列名-文章标题](相对路径)` 引用，只用 1-2 句概述核心结论
- 当涉及其他系列未覆盖的内容时，简要说明核心逻辑（3-5 句），不要深入展开
- 涉及引用时，必须在文章开头"本篇定位"中显式声明

## 章节规划
请按照以下章节规划撰写：

1. 【章节 1 标题】
   - 要讲的内容：...
   - 核心源码路径（含内核版本基线）：...
   - 关联的工程问题：...

2. 【章节 2 标题】
   - 要讲的内容：...
   - 核心源码路径（含内核版本基线）：...
   - 关联的工程问题：...

...（按需添加）

## 禁止事项
- 禁止写成面试八股（罗列概念但不深入）
- 禁止上来就贴源码而不给上下文
- 禁止只讲原理不关联实际问题
- 禁止使用"据说""通常认为""一般来说"等模糊表述，要么给源码依据，要么给数据依据
- 禁止编造不存在的源码文件路径（不确定时用 `// 路径待确认` 标注，并在附录 B 中记录）
- 禁止混用不同 Android 版本 / 内核版本的 API 而不标注版本
- 禁止在篇末缺少"总结"和"附录 A/B/C/D"（按需）
- 禁止在篇末缺少下一篇衔接（系列最后一篇除外）
- 禁止图表密度不达标（少于 4 张或多于 6 张核心图）
- 禁止出现"先挖坑后填"（如"我们将在后续文章中详细讲"）—— 既然挖了就要当场讲清楚
````

---

## 四、质量检查清单

文章写完后，用以下清单自查（共 26 项，4 个维度）：

### 4.1 内容质量（10 项）

| # | 检查项 | 通过标准 |
|---|--------|---------|
| 1 | **有没有回答"是什么"** | 文章开头 2 段内能让读者明白这个东西的定位和职责 |
| 2 | **有没有回答"为什么"** | 解释了这个设计选择的背景和动机，而不仅仅是描述现状 |
| 3 | **有没有架构图/层级图** | 至少有一张 ASCII Art 图说明该模块在系统中的位置和上下游关系 |
| 4 | **源码有没有标路径+版本基线** | 每段源码都标注了对应的文件路径，且标注了 AOSP/内核 版本基线 |
| 5 | **源码前有没有上下文** | 每段源码前都有自然语言解释"这段代码要干什么" |
| 6 | **有没有关联实际问题** | 每个知识点都能回答"这和我排查线上问题有什么关系" |
| 7 | **有没有实战案例** | 文章末尾有 1-2 个完整的排查案例（现象→分析→根因→修复） |
| 8 | **案例是否可验证** | 案例含 logcat/dmesg/systrace 片段、Android+内核版本、复现步骤、修复 diff |
| 9 | **深度够不够** | 不是停留在 API 使用层面，而是深入到数据结构和算法层面 |
| 10 | **广度够不够** | 覆盖了该主题的主要方面，没有重大遗漏 |

### 4.2 结构完整性（6 项）

| # | 检查项 | 通过标准 |
|---|--------|---------|
| 11 | **有没有本篇定位声明** | 文章开头有"本篇定位"段，明确系列角色、强依赖、衔接、不重复内容 |
| 12 | **有没有总结** | 篇末有 3-5 条架构师视角的关键 Takeaway |
| 13 | **有没有附录 A：源码索引** | 篇末有表格列出本篇涉及的所有源码文件 + 内核版本基线 |
| 14 | **有没有附录 B：路径对账表** | 篇末有对账表，逐条标注【已校对/待确认】+ 校对来源 |
| 15 | **有没有附录 C：量化自检表** | 篇末有量化数据自检表，所有数量级都标注依据 |
| 16 | **有没有附录 D：工程基线表**（按需） | 若涉及可调参数，必有工程基线表 |

### 4.3 系列一致性（5 项）

| # | 检查项 | 通过标准 |
|---|--------|---------|
| 17 | **跨篇引用是否到位** | 涉及本系列其他篇章时用 Markdown 链接引用，没有重复展开 |
| 18 | **跨系列引用是否到位** | 涉及其他系列时用相对路径链接，只概述核心结论 |
| 19 | **术语是否一致** | 同一个概念在系列所有文章中使用相同名称 |
| 20 | **AOSP 版本是否统一** | 应用层/Framework 基线统一为 AOSP android-14.0.0_r1 |
| 21 | **内核版本是否统一** | Linux 内核基线在多版本矩阵中明确标注 |

### 4.4 AI 生成质量（5 项）

| # | 检查项 | 通过标准 |
|---|--------|---------|
| 22 | **源码路径是否真实** | 附录 B 对账表全量核对完毕（不仅抽查 3-5 个）；校对来源 cs.android.com / elixir.bootlin.com / LXR |
| 23 | **API 版本是否正确** | 源码中的方法签名和类名与目标 AOSP/内核 版本一致 |
| 24 | **量化描述是否具体** | 附录 C 自检表覆盖全文所有数量级；无"大约""通常""一般来说" |
| 25 | **案例是否标注类型** | 标注了"典型模式"或"真实案例（来源：...）" |
| 26 | **图表密度是否达标** | 4-6 张核心图；平均每 1500 字 1 张 |

---

## 五、反例库（升级为可检索结构）

> 集中收纳过往系列已踩过的反例，**按"症状关键词"反向索引**。后续系列写文章前先查一遍，避免重蹈覆辙。

### 5.1 反向索引

| 症状关键词 | 反例 # | 触发原因 | 防范规则 |
|-----------|--------|----------|----------|
| 「源码路径幻觉」 | #3 | AI 基于旧版本训练数据生成不存在的路径 | 附录 B 全量对账 |
| 「AOSP 版本混用」 | #4 | 不同 Android 版本 API 命名不同 | 标注版本基线，多版本差异用对比表 |
| 「模糊量化」 | #5 | "通常/大约"没有依据 | 附录 C 量化自检 |
| 「纯科普模式」 | #1 | 没有源码、没有数据结构、没有关联问题 | 硬性要求每章三件套（定义/源码/风险） |
| 「代码堆砌模式」 | #2 | 上来贴源码不解释 | 强制"贴代码前自然语言+贴代码后视角" |
| 「图表过密/过稀」 | #6 | 缺密度规则约束 | 4-6 张图，1500 字/张 |
| 「工程参数无基线」 | #7 | "maxevents 该设多大"读者得不到答案 | 附录 D 工程基线表 |
| 「案例不可验证」 | #8 | 只讲"线上遇到一个问题"没有 dump/版本/复现 | 案例可验证性 4 件套 |
| 「跨篇重复造内容」 | #9 | 同一概念在多篇中重复展开 | 本篇定位段 + 跨篇引用规范 |
| 「挖坑不填」 | #10 | "我们将在后续文章详细讲" | 禁止挖坑不填 |

### 5.2 反面教材全文

#### 反例 #1：纯科普模式
```
❌ "ART 是 Android 的运行时环境，负责执行 Java 代码。它支持 JIT 和 AOT 两种编译模式..."
```
问题：没有源码、没有数据结构、没有关联问题，停留在百科词条级别。

#### 反例 #2：代码堆砌模式
```
❌ "## 1. 源码结构
   源码路径：art/runtime/runtime.cc

   ```cpp
   bool Runtime::Init(...) {
     heap_ = new gc::Heap(...);
     ...
   }
   ```"
```
问题：上来就贴源码，没有解释"为什么要看这段代码"、"它解决什么问题"、"它和我的日常工作有什么关系"。

#### 反例 #3：源码路径幻觉
```
❌ 源码路径：frameworks/base/services/core/java/com/android/server/wm/WindowManagerPolicy.java
```
问题：这个文件在 Android 10+ 已被移除/重构，AI 基于旧版本训练数据生成了不存在的路径。

**防范方法**：附录 B 全量对账，逐条在 [Android Code Search](https://cs.android.com/) / [Elixir](https://elixir.bootlin.com/) / LXR 上验证。不确定时要求 AI 标注 `// 路径待确认` 并在附录 B 中记录。

#### 反例 #4：版本混用
```
❌ "InputDispatcher 通过 InputMonitor.updateInputWindowsLw() 同步窗口信息..."
   （后面又写）
   "InputDispatcher 通过 InputWindowHandle.setInputWindows() 更新..."
```
问题：第一种写法是 Android 11 之前的，第二种是 Android 12+ 的。混在一起会让读者困惑。

**防范方法**：在一站式模板中指定多版本基线矩阵。涉及版本差异时，用以下格式：

```
// Android 11 及之前
InputMonitor.updateInputWindowsLw()

// Android 12+ （当前版本）
InputMonitor.updateInputWindows(Transaction)
```

#### 反例 #5：模糊量化
```
❌ "InputDispatcher 通常在几毫秒内完成事件分发"
❌ "大约 60% 的 ANR 与 Input 相关"
```
问题："通常""大约"没有依据。要么给源码中的常量值，要么给具体的统计数据。

**正确姿势**：
```
✅ "InputDispatcher 的 dispatchOnce() 单次循环耗时在 1-3ms 级别（无锁竞争时）"
✅ "Input ANR 占线上 ANR 总量的 60%+（基于 XXX App 2024 年线上数据）"
```

#### 反例 #6：图表密度失控
```
❌ 全文 8000 字只有 1 张架构图 → 信息密度跟不上
❌ 全文 8000 字塞了 12 张图 → 图淹没文字
```
**防范方法**：4-6 张核心图，平均 1500 字/张。

#### 反例 #7：工程参数无基线
```
❌ "epoll_wait 第二个参数 maxevents 应该设置一个合适的值"
```
问题：读者不知道"合适"是多大，也不知道"不合适"会发生什么。

**正确姿势**（在附录 D 中给）：
```
| 参数 | 典型默认 | 选用准则 | 踩坑提醒 |
| maxevents | 64 | 高吞吐场景可提到 128-256 | 太小→多次 epoll_wait 调用；太大→单次返回 fd 多但处理慢；与单次事件处理量平衡 |
```

#### 反例 #8：案例不可验证
```
❌ "曾经遇到一个线上问题，InputDispatcher 阻塞导致 ANR"
```
问题：没有版本、没有 logcat、无法复现。

**正确姿势**：
```
✅ 环境：Android 14 (AOSP 14.0.0_r1) / Kernel 5.10 / 设备 Pixel 6
   复现：滑动列表时 100Hz 触摸事件注入
   logcat 关键片段：
   ```
   E/InputDispatcher: Channel 'xxxxx' ~ Consumer is not responding
   W/ActivityManager: Input dispatching timed out
   ```
   根因：app 端主线程 Looper 在 onTouchEvent 中做了 30ms 同步操作
   修复：将 onTouchEvent 改为 post 到异步线程
```

#### 反例 #9：跨篇重复造内容
```
❌ 在 epoll 文章中详细讲了"什么是 fd、什么是 poll/select"，但同系列 socket-04 已经详细讲过
```
**防范方法**：本篇定位段强制声明"不重复内容"，跨篇用 Markdown 引用。

#### 反例 #10：挖坑不填
```
❌ "关于 epoll 在 Android 中的实际应用，限于篇幅我们将在后续文章中详细讨论。"
```
问题：架构师读者最关心"实际应用"，你说"后续"就是最严重的反例。

**正确姿势**：要么当场讲清楚，要么明确写"本篇不涉及，详见 [系列名-文章](path)"。

#### 正确姿势：自顶向下 + 有据可查
```
✅ "## 1. ART 是什么
   ART 是 Android 的核心运行时引擎，负责...（定义）

   ## 2. 为什么需要它
   在 ART 之前，Android 使用 Dalvik，存在以下痛点...（背景）

   ## 3. 它在系统中的位置
   ART 位于 Framework 之下、Native 库之上...（ASCII 架构图）

   ## 4. 核心机制
   ART 的启动流程始于 Zygote 调用 JNI_CreateJavaVM...
   - 源码路径：art/runtime/java_vm_ext.cc / 内核版本基线 AOSP 14.0.0_r1
   - （贴代码前）这个函数做了三件事：创建 Runtime → 启动 → 绑定主线程
   - （贴代码后）稳定性视角：如果 Runtime::Start 失败，Zygote 崩溃，系统反复重启

   ## 5. 实战案例
   现象：线上大量 TimeoutException...
   环境：Android 13 (AOSP 13.0.0_r1) / Kernel 5.10 / 设备 Pixel 5
   分析：FinalizerWatchdogDaemon 检测到...
   logcat 关键片段：...
   根因：finalize() 中获取了竞争激烈的锁...
   修复：commit hash xxxxxx

   ## 总结
   1. ART 稳定性的三个核心关注点是...
   2. ...

   ## 附录 A：核心源码路径索引
   | 文件 | 路径 | 内核版本基线 | 说明 |
   | ... | ... | ... | ... |

   ## 附录 B：源码路径对账表
   | 序号 | 路径 | 状态 | 校对来源 |
   | 1 | art/runtime/runtime.cc | 已校对 | cs.android.com/android-14.0.0_r1 |
   | ... |

   ## 附录 C：量化数据自检表
   | 序号 | 量化描述 | 数量级 | 依据 |
   | 1 | ART 启动总耗时 | 800-1500ms | Pixel 实测；zygote_article 06 节 |
   | ... |

   下一篇 [02-ART 字节码](02-ART字节码.md) 将深入..."
```

---

## 六、适用场景示例

本指南已成功用于以下模块的系列文章生成：

- **ART 系列**（8 篇）：总览 → 字节码 → 编译执行 → 类加载 → 内存/GC → JNI → 对比JVM → 演进
- **Input 系列**（8 篇）：总览 → EventHub/InputReader → InputDispatcher → InputChannel → View事件分发 → Input ANR → 风险全景 → 诊断工具
- **MM_v2 系列**（13 篇）：内存系统总览 → 进程内存地图 → ART 堆 → Native 堆 → AMS 治理 → LMKD → PSI → Node/Zone → 伙伴系统 → SLAB → 回收 → 风险全景 → 诊断工具
- **IO 系列**（9+1 篇）：总览 → 优先级与交互 → VFS → Page Cache → 块层 → Android 存储栈 → 进程阻塞 → 风险全景 → 诊断工具；延伸专题（进程优先级与 IO 优先级）
- **Socket 系列**（8 篇规划中）：总览 → API 与数据结构 → 生命周期 → 缓冲区 → backlog → UDS 与 Android → 风险全景 → 诊断工具
- **Window 系列**（11 篇规划中）
- **epoll 横切专题**（1 篇已落地，[01-epoll 总览与核心机制](Linux_Kernel/socket/epoll/01-epoll总览与核心机制.md)）：定义与历史 → 三态结构 → ET/LT 源码 → Android 应用 → 风险地图 → 2 案例；机制与案例一篇覆盖，不再拆 02/03

### 使用流程速查

```
第一步：定义角色与目标
    ↓
第二步：让 AI 出大纲（审核，可能迭代 1-2 轮）
    ↓
第三步：生成系列 README（审核）
    ↓
第四步：逐篇撰写（每篇使用一站式模板，写完用 26 项质量清单自查）
    ↓
收尾 1：全系列完成后，更新 README 的链接和阅读建议
    ↓
收尾 2（硬规则）：git add . && git commit
    提交信息规范：
    <系列名> <篇数> 按 v3 指南全部重写/新增
      （<已完成>/<规划>=<完成率>%, 合计 <行数>行 / <字数>字）
    详见 [../../AGENTS.md §1.1](../../AGENTS.md)
```

> **为什么"git 提交"是硬规则**（2026-06-26 由用户明确指示，沉淀为长期规则）：
> - 系列文章是"一次性写完"的产物，中间没有 PR review 流程，**写完不 commit = 临时工作可能因环境问题丢失**
> - 提交是天然的"存档点"，方便后续 `--amend` 修正或 `git diff` 追踪
> - commit 信息携带"完成度 + 行数 + 字数"，是项目进度最权威的进度看板
> - 已验证 6 个系列（PLE / MM_v2 / IO / Socket / Input_FWK / Watchdog 累计 6/6 = 51.7% 合规率）
>
> **违反这条规则 = 任务失败**。写完系列必须**立即** commit，**不要**等用户提醒、**不要**合并到下一个系列。

---

## 附录：v2 → v3 变更记录

| 变更项 | v2 | v3 | 变更原因 |
|--------|----|----|---------|
| 版本基线 | 仅 AOSP 14 | AOSP 14 + Linux kernel 5.10/5.15/6.1/6.6 多版本矩阵 | epoll/调度/信号等内容跨内核版本差异显著 |
| 反面教材 | 散落两节 | 反例库 + 反向索引 | 跨系列复用，按症状关键词快速检索 |
| 图表密度 | "画架构图"无密度规则 | 4-6 张图、1500 字/张 | 防过密/过稀 |
| 工程基线 | 无 | 强制附录 D（参数/默认/准则/踩坑） | 架构师读者要"我线上该用啥" |
| 量化数据 | 抽查 | 强制附录 C 量化自检 | 全文数量级都可追溯 |
| 源码路径 | 抽查 3-5 个 | 强制附录 B 全量对账 | 改抽查为全量 |
| 本篇定位 | 无 | 模板开头强制 | 减少跨篇重复 |
| 案例可验证 | 仅"典型/真实"标注 | 含 dump/版本/复现/diff | "线上案例"要能复现 |
| 质量清单 | 21 项 | 26 项 | 新增硬件/基线/可验证性维度 |
| README 模板 | 5 段 | 6 段（加质量基线段） | 横切型系列需要 |
